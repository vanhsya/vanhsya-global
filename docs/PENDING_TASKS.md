# Pending Tasks & Requirements

This document tracks the remaining work required to bring the platform to full production maturity.

## 🛠️ Technical Debt & Fixes
- [ ] **CV Template Previews**: Currently using placeholders in `src/app/ai-innovations/page.tsx`. Need to upload actual `.png` previews to `public/cv-templates/`.
- [ ] **Google Analytics**: The verification code in `layout.tsx` is a placeholder. Need to replace with the live tracking ID.
- [ ] **API Integrations**: The SOP Generator and AI Chatbot currently use simulated logic. Need to connect to real LLM endpoints (e.g., OpenAI or Anthropic).

## 🚀 Performance & SEO
- [ ] **Turbopack Migration**: Convert custom Webpack configuration in `next.config.js` to Turbopack-compatible rules for faster development.
- [ ] **Structured Data Verification**: Validate all JSON-LD schemas generated by `SEO.tsx` using the Schema Markup Validator.
- [ ] **PWA Support**: Implement `manifest.json` and service workers for offline accessibility and "Add to Home Screen" functionality.

## 📝 Content & UX
- [ ] **Video Content**: Replace the `ideo` video pointer with the final high-resolution background video in `public/videos/`.
- [ ] **Multi-language Support**: Add i18n support for major regions (e.g., Arabic for UAE, Hindi for South Asia).
- [ ] **Client Portal Backend**: Build the actual dashboard functionality for the `/portal` route, including document upload and status tracking.

## ✅ Quality Assurance
- [ ] **Cross-Browser Testing**: Conduct full visual regression tests on Safari (iOS) and Firefox.
- [ ] **Accessibility Audit**: Run a full Axe-core audit to ensure 100% WCAG 2.1 compliance.
- [ ] **Load Testing**: Test the performance of the WebGL hero section on low-end mobile devices.
